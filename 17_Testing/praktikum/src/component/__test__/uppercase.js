import React, { useState } from 'react'
import NameForm  from "../FormCoding"

export const Uppercase = () => {
  const [upper, setUppercase] = useState("")
  
  const handleUppercase = () => {
    return(
      <NameForm />
    )
  }
  
  return(
  )
}
